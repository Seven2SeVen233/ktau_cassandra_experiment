name: ktau-experiments
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.26
  - pandas>=2.0
  - pyyaml>=6.0
  - pip
  - pip:
      - cassandra-driver==3.30.1
      # 可选：图表复现
      # - matplotlib>=3.8
